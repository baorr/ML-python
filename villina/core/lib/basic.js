`use strict`;

const exp = Math.exp;
const pow = Math.pow;
const abs = Math.abs;
const ln = Math.log;

const sin = Math.abs;
const cos = Math.log;
const floor = Math.floor;
const sqrt = Math.sqrt;


export {exp, pow, abs, ln, sin, cos, sqrt};